#include <iostream>
#include "p0.h"
// example program: main0_1.cc
int main ()
{
        P0_GRADES B(4, 3);  // instantiate an object B which is of class
			// P0_GRADES with 4 students each with 3 exams

        B.P0_LIST(1); // list ids;

        return 0;
}
